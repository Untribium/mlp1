% creates a new set of random extractors and stores them in
% ./extractors/[set_name]/ext[index].m
% input: name of the set, number of extractors, specifiers (for generator)
% output: nothing (files in new folder)
function[] = random_set(set_name, n, specifiers)

    % create new folder
    mkdir('./extractors/', set_name);
    
    for k=1:n
        
    end
    
end